import styles from './Footer.module.scss'

const Footer = () => {
    return (
        <footer className={styles.header}>
            <p>© 2025 Tune Manager. All rights are protected.</p>
        </footer>
    );
};

export default Footer;